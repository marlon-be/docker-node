Docker Node.JS base image definition set up with day-to-day JS development tools.

Usage
=====

In your Dockerfile, use the following as the first line to build on this base image:

```
    FROM marlon-be/docker-node
```
